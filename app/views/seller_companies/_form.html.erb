<%= form_for(@seller_company) do |f| %>
  <% if @seller_company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@seller_company.errors.count, "error") %> prohibited this seller_company from being saved:</h2>

      <ul>
      <% @seller_company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
